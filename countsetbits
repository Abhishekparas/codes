#include<iostream>
using namespace std;
int main(){
	int n,num;
	cin>>n;
	for(int i=0 ; i<n; i++){
		int count = 0;
		cin>>num;
		while (num) { 
        num = num & (num-1);
		count++;
    	} 
    cout<<count<<endl;
	}
	return 0;
	
}
