#include <bits/stdc++.h>
using namespace std;
vector<char> v;
int main()
{
    string str;
    cin>>str;
	for(int i=0;i <str.length(); i++){
		if(str[i] != str[i+1]){
			v.push_back(str[i]);
		}
		
	}
	for(auto x:v){
		cout<<x;
	}

    return 0;
}
