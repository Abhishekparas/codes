#include<bits/stdc++.h>
using namespace std;
int main()
{
    long int input,sum;
    cin>>input;
    sum=input;
    while(sum>=0)
    {
        cout<<input<<endl;;
        cin>>input;
        sum+=input;
    }
    

    
    return 0;
}
